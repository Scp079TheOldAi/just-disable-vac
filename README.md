# vacbypass-modules

Steam client VAC bypass preload (`libvpcfs.so.0`). Part of the [Rosnehook](https://github.com/Scp079TheOldAi) bot stack.

```bash
git clone https://github.com/Scp079TheOldAi/vacbypass-modules.git
cd vacbypass-modules
mkdir -p build && cd build && cmake .. && make
```

Output: `build/bin64/libvpcfs.so.0`